# Gece Çalışma Günlüğü — 2026-06-07

Kullanıcısız gece çalışması. Kural: her iş → PR → CI → backend/test/docs **merge
edildi**, frontend **yeşil bırakıldı** (sabah görsel inceleme için). Aşağıda
sabah yapman gerekenler net.

---

## ✅ main'e MERGE edildi (CI yeşil)

| PR | Ne | Tür |
|----|----|----|
| #117 | `docs/FOOTBALL_GLOSSARY.md` — xG/xGA/CCA/OP/yarı alan/gegenpress/roller, motorlara bağlı | docs |
| #119 | Konsolidasyon API kontrat testi (B uçları + A deprecation + rating) | test |
| #120 | `docs/SCREEN_INVENTORY.md` — 10 ekranın gerçek durumu | docs |
| #122 | Norm derecesi — 13 protokolün tümü uç-değer kilidi | test |

## 🟢 YEŞİL — senin incelemen için bekliyor (frontend, merge etmedim)

| PR | Ekran/İş | Endpoint | Sabah |
|----|----------|----------|-------|
| #118 | **Tasarım temeli**: `brand` token + `EndpointTag`/`RiskPill`/`ConditionBar`/`ProbBar` | — | Görsel yok (token+primitif). Güvenli; merge edilebilir. |
| #121 | **Tıbbi Merkez** (`/medical`) — rehab/return-to-play | `/players/{id}/rehab` | Görsel bak + merge |
| #123 | **Scout** (`/scout`) — oyuncu benzerliği + watchlist | `/admin/scout/*` | Görsel bak + merge |
| #124 | **Maç Planı** (`/match-plan`) — canlı senaryo | `/matches/{id}/plan/vs-live` | Görsel bak; in-language ilk sürüm |

> **Merge çakışma notu:** #121/#123/#124 hepsi `sidebar.tsx` + `i18n.tsx`'e nav
> satırı ekliyor. İlkini merge ettikten sonra diğerlerinde küçük çakışma çıkabilir
> → **çözüm: her iki nav satırını da koru** (önemsiz). #118'i ilk merge et (temel),
> sonra ekranlar.

---

## 🔭 Sabah senin kararın / aksiyonun

1. **Frontend PR'larını görsel incele + merge et** (yukarıdaki sıra).
2. **PDF görsel onayı** (önceki iş, #115 main'de): `/physical-tests` → bir oyuncu
   → "PDF indir" → düzgünse "adım 4" de, `/performance`'ı tam redirect ederim.
3. **GitHub ayarları (kod değil):**
   - Branch protection: `main` için CI zorunlu (merge-before-green döngüsünü bitirir).
   - GitHub App'e `actions: write` (flaky CI'leri otomatik re-run edebilmem için).
4. **Referans HTML bekleyen ekranlar:** Ana Konsol (FM 3-kolon), Canlı Maç Konsolu,
   ve tam Maç Planı (önerilen 11 + rakip brifing). Dosyaları verince birebir uygularım.

## Hâlâ eksik (referans gelince)
- Ana Konsol (Genel Bakış) — `/` dashboard'u FM 3-kolona çevir.
- Canlı Maç Konsolu — `/live` (5 motor + saha + momentum + audit).
- Maç Planı tam sürüm — #124 üstüne önerilen 11 + eşleşme grid.

## Tasarım dili (sabit)
DESIGN.md token'ları + `FOOTBALL_GLOSSARY.md` + FM primitifleri (#118). Sayılar
monospace, her panelde endpoint etiketi, marka rengi `--brand` (tenant-dinamik).
