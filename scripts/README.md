# scripts/

Komut satırından çalıştırılan operasyonel araçlar.

**Faz 1'de eklenecek:**
- `sync_league.py` — bir ligi çeker, doğrular, DB'ye yazar, snapshot alır,
  takımları listeler. Argümanlar: `--league <id> --season <yıl>`.
