fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
alembic
psycopg[binary]
httpx
anthropic

# auth — JWT + bcrypt for multi-tenant + role-based access
bcrypt>=4.0
PyJWT>=2.7

# ML — xG real training (Prompt 2, Faz N)
# scikit-learn for LogisticRegression, joblib for model artifact serialize
scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
joblib>=1.3

# test (geliştirme)
pytest
