fastapi
uvicorn[standard]
pydantic
pydantic-settings
sqlalchemy
alembic
psycopg[binary]
httpx
anthropic

# auth — JWT + bcrypt for multi-tenant + role-based access
bcrypt>=4.0
PyJWT>=2.7

# test (geliştirme)
pytest
